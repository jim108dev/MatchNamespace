# MatchNamespace

```txt
USAGE: MatchNamespace [--help] [--nothing] [--prefix <path>] <path>

ROOT:

    <path>                Specify a the root directory.

OPTIONS:

    --nothing, -n         Just print the results but do not alter file contents.
    --prefix, -p <path>   Specify a namespace prefix.
    --help                display this list of options.
```
